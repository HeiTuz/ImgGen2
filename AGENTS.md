# AGENTS.md — repository operating rules

This repository is the canonical editable source for ImgGen2. **Any agent (Claude Code, Codex, Hermes) may edit and locally commit in `~/HeiTuz/ImgGen2` at any time** (amended 2026-08-04, superseding the 2026-08-02 Codex-exclusive boundary). Codex is the primary steward — consistency checks, install regeneration, doctrine parity, release preparation — not the sole editor. **Whoever edits must leave a handoff** at `~/handoffs/<repo>-<topic>-<timestamp>.md` covering what changed and why, files touched, commit hash, whether installs need regenerating, what was and was not verified, and remaining work; either commit locally or state in the handoff that changes are uncommitted. Installed payloads must not be hand-edited — edit this tree and regenerate. The Codex install at `~/.codex/skills/ImgGen2` is a copied `agents/codex/` overlay artifact, not a second source of truth. Version bumps and release pushes remain separately approved.

After a canonical change, regenerate the relevant host payloads and verify overlay parity. Version bumps and release pushes are separate release actions; do not push without explicit release approval. Do not edit `~/src/MPW-release`, remove skills, or change contracts/schema/mirrors without the approval boundary in the task brief.

The repository root (`SKILL.md`, `references/`, `scripts/`, `contracts/`, and `examples/`) is the behavioral source. `agents/<host>/` changes only host integration framing and must not fork the rules. Run the repository's documented verification commands after changes.
